#include <iostream>
#include <string>
using namespace std;
bool pa(int c)
{
    string str;
    str = to_string(c);
    for(int i=0;i<str.length()/2;i++)
    
}
int main()
{
    int n;
    cin >> n;
    for (int i = 0; i < n; i++)
    {
        int times, data;
        cin >> data;
    }
}