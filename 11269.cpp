#include <iostream>
using namespace std;
#include <vector>
int main()
{
    int n;
    while (cin >> n)
    {
        vector<int> s(n);
        vector<int> g(n);
        for (int i = 0; i < n; i++)
            cin >> s[i];
        for (int i = 0; i < n; i++)
            cin >> g[i];
        
    }
}